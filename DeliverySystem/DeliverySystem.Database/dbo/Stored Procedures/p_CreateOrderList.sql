CREATE PROCEDURE [dbo].[p_CreateOrderList]
	@DeeliveryQueueItemIds NVARCHAR(255),
	@param2 int
AS
	-- Insert into Замовлення

	-- DECLARE @ORderId = SCOPE_IDENTITY()

	 -- insert into OrderItems

	 -- update DeliveryQueue set IsCompleted = 1 where Id in @DeeliveryQueueItemIds
RETURN 0
